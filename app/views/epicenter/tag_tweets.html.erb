<div>
    <h1><%= @tag.phrase %></h1>
</div>

<div class="well">
  <div class="row">
    <!-- a place for current user stats -->
    <div>
      <% @tag.tweets.order(created_at: :desc).each do |tweet| %>
          <p> 
  	    <%= link_to tweet.user.name, show_user_path(id: tweet.user.id) %> <%= "@#{tweet.user.username}" %>
  	    • <%= time_ago_in_words(tweet.created_at) %>
          </p>
  	<p>
  	    <%= tweet.message.html_safe %>
  	</p>
      <% end %>
     </div>
  </div>
 </div> 